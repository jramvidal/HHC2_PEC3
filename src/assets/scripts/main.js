// Import Font Awesome icons (CSS only, no JS functionality needed)
import "@fortawesome/fontawesome-free/css/all.min.css";

// Import Tailwind CSS directives (base, components, utilities)
import "../styles/tailwind.css";

// Import custom SCSS styles (includes reusable classes, page-specific styles, etc.)
import "../styles/main.scss";
